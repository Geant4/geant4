{
  // This script compares the output generated by FORTRAN and C++
  // versions of INCL4.

  // Input data:
  TString fortranFileName("../data/pb208proton1gev_withabla.root");
  TString cppFileName("testevap.root");

  TFile *fortranFile = new TFile(fortranFileName);
  TFile *cppFile = new TFile(cppFileName);

  TTree *fortranTree = (TTree*) fortranFile->Get("h101");
  TTree *cppTree = (TTree*) cppFile->Get("h101");

  fortranTree->SetLineColor(kRed);
  
  TCanvas *canvas = new TCanvas();

  cppTree->Draw("Avv", "", "");
  fortranTree->Draw("Avv", "", "same");
  canvas->SetLogy(1);
  canvas->Print("testevap.ps(");
  canvas->SetLogy(0);

  cppTree->Draw("Zvv", "", "");
  fortranTree->Draw("Zvv", "", "same");
  canvas->SetLogy(1);
  canvas->Print("testevap.ps");
  canvas->SetLogy(0);
  
  canvas->Clear();
  cppTree->Draw("Enerj", "Avv == 1 && Zvv == 0", "");
  fortranTree->Draw("Enerj", "Avv == 1 && Zvv == 0", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Plab", "Avv == 1 && Zvv == 0", "");
  fortranTree->Draw("Plab", "Avv == 1 && Zvv == 0", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Tetlab", "Avv == 1 && Zvv == 0", "");
  fortranTree->Draw("Tetlab", "Avv == 1 && Zvv == 0", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Philab", "Avv == 1 && Zvv == 0", "");
  fortranTree->Draw("Philab", "Avv == 1 && Zvv == 0", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Enerj", "Avv == 1 && Zvv == 1", "");
  fortranTree->Draw("Enerj", "Avv == 1 && Zvv == 1", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Plab", "Avv == 1 && Zvv == 1", "");
  fortranTree->Draw("Plab", "Avv == 1 && Zvv == 1", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Tetlab", "Avv == 1 && Zvv == 1", "");
  fortranTree->Draw("Tetlab", "Avv == 1 && Zvv == 1", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Philab", "Avv == 1 && Zvv == 1", "");
  fortranTree->Draw("Philab", "Avv == 1 && Zvv == 1", "same");
  canvas->Print("testevap.ps");
  
  canvas->Clear();
  cppTree->Draw("Enerj", "Avv == 4 && Zvv == 2", "");
  fortranTree->Draw("Enerj", "Avv == 4 && Zvv == 2", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Plab", "Avv == 4 && Zvv == 2", "");
  fortranTree->Draw("Plab", "Avv == 4 && Zvv == 2", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Tetlab", "Avv == 4 && Zvv == 2", "");
  fortranTree->Draw("Tetlab", "Avv == 4 && Zvv == 2", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Philab", "Avv == 4 && Zvv == 2", "");
  fortranTree->Draw("Philab", "Avv == 4 && Zvv == 2", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Plab", "Avv > 5", "");
  fortranTree->Draw("Plab", "Avv > 5", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Tetlab", "Avv > 5", "");
  fortranTree->Draw("Tetlab", "Avv > 5", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Philab", "Avv > 5", "");
  fortranTree->Draw("Philab", "Avv > 5", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Enerj", "Avv > 5", "");
  fortranTree->Draw("Enerj", "Avv > 5", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Plab", "", "");
  fortranTree->Draw("Plab", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Tetlab", "", "");
  fortranTree->Draw("Tetlab", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Philab", "", "");
  fortranTree->Draw("Philab", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Enerj", "", "");
  fortranTree->Draw("Enerj", "", "same");
  canvas->SetLogy(1);
  canvas->Print("testevap.ps");
  canvas->SetLogy(0);
  
  canvas->Clear();
  fortranTree->Draw("Massini", "", "");
  cppTree->Draw("Massini", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  fortranTree->Draw("Mzini", "", "");
  cppTree->Draw("Mzini", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  fortranTree->Draw("Exini", "", "");
  cppTree->Draw("Exini", "", "same");
  canvas->Print("testevap.ps");
  
  canvas->Clear();
  fortranTree->Draw("Jremn", "", "");
  cppTree->Draw("Jremn", "", "same");
  canvas->Print("testevap.ps");

  canvas->Clear();
  cppTree->Draw("Bimpact", "", "");
  fortranTree->Draw("Bimpact", "", "same");
  canvas->Print("testevap.ps)");

  delete canvas;
  exit(0);
}

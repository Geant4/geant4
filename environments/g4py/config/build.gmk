# $Id: build.gmk,v 1.1 2006-02-27 09:35:39 kmura Exp $
# ===========================================================
#   Script for building a library
# ===========================================================
headers := $(wildcard *.hh)
sources := $(wildcard *.cc)
objects := $(patsubst %.cc, %.o, $(sources))

.PHONY: clean debug 

module_name := $(MODULE).$(soext)
$(module_name) : $(objects)
	$(build-module)

module : $(module_name)

clean :
	@-rm -f $(module_name)
	@-rm -f $(objects)

debug: 
	@echo debug...

